import PropTypes from 'prop-types'

const Hangman = ({ cards }) => {
  return(
    <div>
      Nothing here yet
    </div>
  )
}

Hangman.propTypes = {
  cards: PropTypes.object.isRequired,
}

export default Hangman